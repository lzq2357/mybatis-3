<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd" >
<configuration>
	
	
	<properties>
		<!-- 默认值功能，默认false，即 ${xxx:defaultValue} ，如果变量 xxx 未配置，则使用defaultValue 作为变量值-->
		<property name="org.apache.ibatis.parsing.PropertyParser.enable-default-value" value="true"/>
		
		<!-- 分隔符，默认使用 : -->
		<property name="org.apache.ibatis.parsing.PropertyParser.default-value-separator" value=":"/>
	</properties>
	<plugins>
		<plugin interceptor="liziq.MyInterceptor1"/>
	</plugins>
	
  <environments default="development">
	  <environment id="development">
		  <!-- 事务管理类型，JDBC表示直接使用JDBC的提交和回滚设置，依赖于数据源得到的连接来管理事务 -->
		  <transactionManager type="JDBC" />
		
		  <!-- 数据库连接池,POOLED表示使用数据库连接池 -->
		  <dataSource type="POOLED">
			  <property name="driver" value="com.mysql.jdbc.Driver" />
			  <property name="url" value="jdbc:mysql://localhost:3306/mybatis" />
			  <property name="username" value="root" />
			  <property name="password" value="123456" />
		  </dataSource>
	  </environment>
  </environments>
  
	
  <mappers>
	  <mapper resource="resources/liziq/Mapper.xml" />
  </mappers>
</configuration>